# Copy to .env and fill in. .env is git-ignored and must stay that way.
APP_ENV=dev

DB_HOST=localhost
DB_USER=
DB_PASSWORD=
DB_NAME=crm51_db

# Flask session signing key. Generate a fresh one per environment:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=
