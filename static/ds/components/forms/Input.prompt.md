Text field with optional inline icons and an invalid state.

```jsx
<Input iconStart="search" placeholder="Search customers" />
```

Pair with FormField for labels and errors. `invalid` only paints the border red — the message belongs to FormField.
