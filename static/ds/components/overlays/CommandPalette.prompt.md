Command palette for power users.

```jsx
<CommandPalette open={open} onClose={close} onSelect={run}
  groups={[{ label: 'Go to', items: [{ id: 'customers', label: 'Customers', icon: 'users', shortcut: 'G C' }] }]} />
```
