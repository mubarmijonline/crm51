Bordered panel with optional header, footer and padding control.

```jsx
<Card title="Revenue" subtitle="Last 30 days" actions={<Button size="sm">Export</Button>}>…</Card>
```

Cards are flat by default (hairline border, no shadow). Pass `padded={false}` when wrapping a DataTable so rows meet the border.
