Icon-only square button, ghost by default.

```jsx
<IconButton icon="ellipsis" label="Row actions" />
```

Use `variant="secondary"` when it sits alone next to bordered controls, `active` for toggled toolbar states.
