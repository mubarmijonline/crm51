Bottom sheet — the mobile replacement for dropdown menus and dialogs.

```jsx
<BottomSheet open={open} title="Filter customers" onClose={close} actions={<Button variant="primary" size="lg" fullWidth>Apply</Button>}>…</BottomSheet>
```
