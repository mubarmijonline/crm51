/** 390×844 device frame with a minimal status bar — for presenting mobile screens in specs and kits, not for production. */
export interface PhoneFrameProps { time?: string; children?: React.ReactNode; style?: React.CSSProperties }
export declare function PhoneFrame(props: PhoneFrameProps): JSX.Element;
