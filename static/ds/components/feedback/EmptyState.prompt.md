Empty/zero state with glyph, explanation and a next action.

```jsx
<EmptyState icon="users" title="No customers yet" description="Import a CSV or add one manually." action={<Button variant="primary">Add customer</Button>} />
```
