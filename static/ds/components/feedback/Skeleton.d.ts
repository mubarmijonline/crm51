/** Loading placeholder that mirrors the shape of the content it replaces. Preferred over spinners for page and table loads. */
export interface SkeletonProps { width?: number | string; height?: number | string; radius?: string; lines?: number; style?: React.CSSProperties }
export declare function Skeleton(props: SkeletonProps): JSX.Element;
