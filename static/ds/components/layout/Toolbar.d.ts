/** Page-level control bar above content: filters and view switches on the left, actions on the right. TableToolbar is the in-card variant. */
export interface ToolbarProps { start?: React.ReactNode; end?: React.ReactNode; sticky?: boolean; style?: React.CSSProperties }
export declare function Toolbar(props: ToolbarProps): JSX.Element;
